<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.audiotopapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        >
<!--
        <activity android:name=".AudioLectureActivity"></activity>
-->
        <activity
            android:name=".AudioLectureActivity"
            android:label="Lecture">
<!--
            android:parentActivityName=".ListeAudioActivity"
-->
           <!-- tools:ignore="Instantiatable">-->
           <!-- <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.audiotopapp.ListeAudioActivity" />-->
        </activity>



        <!--         <activity android:name=".ListeAudioActivity"></activity> -->
        <activity
            android:name=".ListeAudioActivity"
            android:label="Liste Audio"
            android:parentActivityName=".MainActivity"
            tools:ignore="Instantiatable">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.audiotopapp.MainActivity" />
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>